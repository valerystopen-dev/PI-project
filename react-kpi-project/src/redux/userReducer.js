import { HIDE_LOADER, SHOW_LOADER, CHANGE_USER_TYPE, COMMON, ADMINISTRATOR, REGISTRATOR } from "./types";

const initialState = {
};

export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
